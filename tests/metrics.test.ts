import { describe, it } from "vitest";

// /api/metrics p50/p95 surface (S3 proof). Owned by T2 (W2). Skipped — do not delete.
describe.skip("GET /api/metrics", () => {
  it("reports p50/p95 + docCount", () => {});
});
