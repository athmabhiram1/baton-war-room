import { describe, it } from "vitest";

// ?fixture=1 answers with canned docs, zero Moss calls. Owned by T6. Skipped — do not delete.
describe.skip("?fixture=1 canned docs", () => {
  it("answers without Moss calls", () => {});
});
